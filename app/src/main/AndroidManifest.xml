<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thetutoringapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".deleteStudent"></activity>
        <activity android:name=".changeColor" />
        <activity android:name=".changeName" />
        <activity android:name=".reportTutor" />
        <activity android:name=".student_messages" />
        <activity android:name=".t_settings" />
        <activity android:name=".s_settings" />
        <activity android:name=".find_tutor" />
        <activity android:name=".picture" />
        <activity android:name=".tutor_dashboard" />
        <activity android:name=".splash" />
        <activity android:name=".studentDashboard" />
        <activity android:name=".tutorRegistration" />
        <activity android:name=".studentRegistration" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>